from django.db import models

class ARIMAModel(models.Model):
    # Definir os campos necessários para o modelo ARIMA
    pass

class ETSModel(models.Model):
    # Definir os campos necessários para o modelo ETS
    pass
